# Reserved Expo client

This folder is intentionally reserved for the later Expo phase. It will consume `@websphere/shared` contracts, the REST API, and Socket.IO event types without duplicating business logic.
